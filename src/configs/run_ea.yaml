defaults:
  - algorithm: nsga2
  - problem: mokp
  - override hydra/job_logging: disabled
  - _self_

track_generations: false
from_pid: 1
to_pid: 3
optimizer: gurobi

hydra:
  run:
    dir: .
  output_subdir: null
